---
title: 활성화 함수
tags:
  - 딥러닝
  - 개념
aliases:
  - activation function
---

신경망 뉴런이 가중합 $W x + b$ 를 받아 출력으로 바꾸는 함수. 핵심 역할은 [[비선형성]]을 부여하는 것이다.

활성화 함수가 없으면 아무리 층을 쌓아도 전체가 1차 함수라 직선밖에 못 긋는다. [[ReLU]] 같은 비선형 함수를 끼우는 순간 곡선을 조합해 거의 모든 함수를 만들 수 있다.

은닉층 기본값은 [[ReLU]]. 관련: [[비선형성]], [[Softmax]].
