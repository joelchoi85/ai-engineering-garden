---
title: 베이즈 정리
tags:
  - 확률통계
  - 개념
publish: true
---

$$P(A\mid B) = \frac{P(B\mid A)\,P(A)}{P(B)}$$

말로 풀면: **"B라는 증거를 봤을 때 A일 확률"** = (A일 때 B가 나올 확률 × 원래 A일 확률) ÷ (B가 나올 전체 확률).

핵심 정신은 **사전 믿음(prior)을 증거로 갱신해 사후 믿음(posterior)을 만드는 것**이다.

```mermaid
flowchart LR
    A[사전: 스팸 비율 20%] --> C[증거: 본문에 '무료']
    C --> B[사후: 스팸 확률 85%로 갱신]
```

스팸 필터가 대표 예다 — "'무료'라는 단어를 보면 스팸 확률을 높인다"는 나이브 베이즈 분류기가 이 정리를 쓴다. 증거가 쌓일수록 믿음을 조정한다는 발상은 머신러닝 전반(베이즈 관점의 학습, 정규화를 사전분포로 보는 시각)에 깔려 있다. 관련: [[최대우도추정]].
