---
title: 행렬곱
tags:
  - 선형대수
  - 개념
---

딥러닝 디버깅의 절반은 이 규칙이다:

$$(N, A) \times (A, B) = (N, B)$$

**앞 행렬의 열 수 = 뒤 행렬의 행 수**여야 곱이 된다. NumPy에선 `@`. [[nn.Linear]]의 정체가 바로 `X @ W.T + b`이고, 신경망 `shape mismatch` 에러의 대부분이 이 규칙 위반이다. 관련: [[행렬]], [[다중 선형 회귀]].
