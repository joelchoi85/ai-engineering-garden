---
title: 상속과 다형성
tags:
  - 파이썬
  - 개념
publish: true
---

**상속**: 공통 기능을 부모 클래스에 두고 자식이 물려받아 중복을 없앤다. **다형성**: 같은 메소드 이름(`speak()`)을 자식마다 다르게 구현 → 부모 타입으로 묶어 호출하면 각자 알맞게 동작.

[[객체지향]]의 핵심 도구. PyTorch에서 `nn.Module`을 상속하는 패턴, sklearn의 `fit/predict` 통일 인터페이스가 모두 이 원리다.
