---
title: 던더 메소드
tags:
  - 파이썬
  - 개념
---

`__이름__` 형태의 특수 메소드. 파이썬 문법이 내 객체에서도 동작하게 해주는 후크다. `a + b`는 사실 `a.__add__(b)` 호출이고, `print(obj)`는 `__str__`을 부른다.

외우는 게 아니라 "이 연산을 내 객체에서 쓰고 싶으면 대응하는 던더를 정의한다"로 이해하면 된다. [[객체지향]] 참고. `model(x)`가 작동하는 건 `__call__` 던더 덕분.
